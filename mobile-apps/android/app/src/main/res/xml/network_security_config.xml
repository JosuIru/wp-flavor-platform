<?xml version="1.0" encoding="utf-8"?>
<!--
    Network Security Configuration for Flavor Chat IA

    SECURITY POLICY:
    - HTTPS required for all production traffic
    - HTTP allowed ONLY for local development (localhost, emulator)
    - System CAs trusted for production
    - Debug builds allow user-installed certificates for testing

    @since 3.1.0
    @security CRITICAL - Do not modify without security review
-->
<network-security-config>
    <!-- Base configuration: HTTPS only, trust system CAs -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>

    <!-- Development: Allow HTTP for local testing ONLY -->
    <domain-config cleartextTrafficPermitted="true">
        <!-- Android emulator localhost -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <!-- iOS simulator / direct device -->
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <!-- Local Sites (desarrollo) -->
        <domain includeSubdomains="true">local</domain>
        <domain includeSubdomains="true">localtest.me</domain>
    </domain-config>

    <!-- Debug builds: Allow user certificates for proxy testing -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system"/>
            <certificates src="user"/>
        </trust-anchors>
    </debug-overrides>
</network-security-config>
